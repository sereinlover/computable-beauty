.PHONY: dev build lint lint-ci

dev:
	npm run dev

build:
	npm run build

lint:
	npm run lint

# Same as lint — eslint never auto-fixes here (no --fix in the script), so
# there's no separate check-only variant to write.
lint-ci:
	npm run lint
