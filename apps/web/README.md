# apps/web

Next.js (App Router) frontend — landing page, analyze/history/result pages, and BFF Route Handlers that proxy to `apps/gateway`.

See the [repository README](../../README.md) for deployment and local dev instructions.
