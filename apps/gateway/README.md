# apps/gateway

Go service — task orchestration, Postgres/Redis, SSE progress, and result aggregation between `apps/web` and `apps/engine`.

See the [repository README](../../README.md) for deployment and local dev instructions.
